#include "monty.h"
/**
 *stack_queue - change the list mode: stack or queue
 *@stack: stack elements
 *@line_number: instruction number
 *
 */
void stack_queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	check_mode('s');
}
